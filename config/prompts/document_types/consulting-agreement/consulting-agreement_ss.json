{
  "title": "Consulting Agreement Extraction",
  "type": "object",
  "properties": {
    "contract_name": {
      "type": "string",
      "description": "The name of the document, e.g., 'Consulting Agreement'."
    },
    "effective_date": {
      "type": "string",
      "format": "date",
      "description": "The date the agreement was signed or became effective."
    },
    "parties": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "name": { "type": "string" },
          "role": {
            "type": "string",
            "enum": ["Client", "Consultant", "Company"]
          }
        },
        "required": ["name", "role"]
      }
    },
    "consulting_details": {
      "type": "object",
      "properties": {
        "scope_of_services_summary": {
          "type": "string",
          "description": "A brief summary of the advisory services the consultant will provide."
        },
        "compensation_summary": {
          "type": "string",
          "description": "A summary of the payment terms (e.g., '$5,000 monthly retainer', '$250 per hour', 'Project-based fee')."
        },
        "term_of_agreement": {
          "type": "string",
          "description": "The duration of the consulting engagement."
        },
        "confidentiality_clause_summary": {
          "type": "string",
          "description": "A brief summary of the confidentiality obligations."
        }
      }
    },
    "discovered_entities": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "entity_name": { "type": "string" },
          "entity_value": { "type": "string" },
          "context_summary": { "type": "string" }
        }
      }
    }
  },
  "required": ["contract_name", "effective_date", "parties", "consulting_details"]
}