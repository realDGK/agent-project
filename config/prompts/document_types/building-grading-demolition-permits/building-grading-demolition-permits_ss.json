{
  "title": "Building/Grading/Demolition Permit Extraction",
  "type": "object",
  "properties": {
    "contract_name": {
      "type": "string",
      "description": "The name of the document, typically 'Building Permit'."
    },
    "effective_date": { "type": "string", "format": "date" },
    "parties": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "name": { "type": "string" },
          "role": {
            "type": "string",
            "enum": ["Permittee", "Owner", "Contractor", "Issuing Agency"]
          }
        },
        "required": ["name", "role"]
      }
    },
    "permit_details": {
      "type": "object",
      "properties": {
        "permit_number": { "type": "string" },
        "permit_type": { "type": "string", "enum": ["Building", "Grading", "Demolition"] },
        "property_address": { "type": "string" },
        "scope_of_work": { "type": "string" },
        "valuation": { "type": "number" },
        "expiration_date": { "type": "string", "format": "date" }
      }
    },
    "discovered_entities": {
        "type": "array",
        "items": {
            "type": "object",
            "properties": {
                "entity_name": { "type": "string" },
                "entity_value": { "type": "string" },
                "context_summary": { "type": "string" }
            }
        }
    }
  },
  "required": ["contract_name", "effective_date", "parties", "permit_details"]
}