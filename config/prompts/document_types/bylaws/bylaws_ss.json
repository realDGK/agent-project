{
  "title": "Corporate Bylaws Extraction",
  "type": "object",
  "properties": {
    "contract_name": {
      "type": "string",
      "description": "The name of the corporation these bylaws govern, e.g., 'Bylaws of [Corporation Name]'."
    },
    "effective_date": {
      "type": "string",
      "format": "date",
      "description": "The date the bylaws were adopted."
    },
    "parties": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "name": { "type": "string" },
          "role": {
            "type": "string",
            "enum": ["Initial Director", "Officer", "Shareholder"]
          },
          "title": { "type": "string" }
        },
        "required": ["name", "role"]
      }
    },
    "governance_details": {
      "type": "object",
      "properties": {
        "director_quorum_requirement": {
          "type": "string",
          "description": "The quorum required for board meetings (e.g., 'A majority of directors', '51%')."
        },
        "shareholder_quorum_requirement": {
          "type": "string",
          "description": "The quorum required for shareholder meetings."
        },
        "director_count": {
          "type": "string",
          "description": "The number of directors or the allowed range (e.g., '3', 'Not less than 3 nor more than 7')."
        },
        "officer_titles": {
          "type": "array",
          "items": { "type": "string" },
          "description": "A list of the officer positions defined (e.g., President, Secretary, Treasurer)."
        },
        "fiscal_year_end": {
          "type": "string",
          "description": "The end date of the corporation's fiscal year (e.g., 'December 31st')."
        },
        "indemnification_clause_summary": {
          "type": "string",
          "description": "A brief summary of the indemnification provisions for directors and officers."
        }
      }
    },
    "discovered_entities": {
        "type": "array",
        "items": {
            "type": "object",
            "properties": {
                "entity_name": { "type": "string" },
                "entity_value": { "type": "string" },
                "context_summary": { "type": "string" }
            }
        }
    }
  },
  "required": ["contract_name", "effective_date"]
}
